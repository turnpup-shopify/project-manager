# Vercel Blob — add this to your Vercel project environment variables too.
# Get the token from: Vercel Dashboard → your project → Storage → Blob → Connect Store
# Then pull it locally with: npx vercel env pull .env.local
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxxxxxxxxxxx
